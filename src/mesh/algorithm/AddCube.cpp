#include "AddCube.hpp"
#include <QtGlobal>

namespace Lattice {
namespace Mesh {

void AddCube::perform(Mesh &mesh) {
    Q_UNUSED(mesh)
    Q_UNUSED(_minPos)
    Q_UNUSED(_maxPos)
    Q_UNUSED(_material)
}

} // namespace Mesh
} // namespace Lattice
